plugins {
    // Lets Gradle auto-download a matching JDK toolchain when none is installed.
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.8.0"
}

rootProject.name = "ktor_voice"
